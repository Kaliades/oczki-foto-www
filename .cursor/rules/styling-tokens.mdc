---
description: Use design tokens, typography utilities and Tailwind 4 conventions
globs: src/**/*.{tsx,ts,css}
alwaysApply: false
---

# Styling Tokens

## Colors

Always use the Oczki CSS variables defined in `src/app/(frontend)/globals.css`:

- `var(--oczki-primary-100..900)` — neutral creams to deep brown
- `var(--oczki-secondary-600)` — sage
- `var(--oczki-tertiary-300|700)` — dusty pink stack
- `var(--oczki-pink)` — accent

```tsx
// ❌ BAD: raw hex from Figma
<p className="text-[#4f3a26]">…</p>

// ✅ GOOD
<p className="text-[var(--oczki-primary-800)]">…</p>
```

If a Figma color is missing, add a new `--oczki-*` token in `:root` of `globals.css` — never reference raw hex from components.

## Typography

Use the utility classes from `globals.css`:
`oczki-heading-l`, `oczki-body-l`, `oczki-body-xl`, `oczki-body-m`, `oczki-body-m-medium`, `oczki-handwritten-s`. They bake in `font-feature-settings` (`ss01`, `ss02`, `lnum`, `pnum`, …) and `font-variation-settings: 'wdth' 100`.

```tsx
// ❌ BAD: bypasses OpenType setup, picks Geist
<h2 className="text-[36px] font-serif">…</h2>

// ✅ GOOD
<h2 className="oczki-heading-l text-[var(--oczki-primary-800)]">…</h2>
```

For font-family in inline ad-hoc spots prefer `[font-family:var(--font-oczki-body)]` — never `font-[var(--…)]` (Tailwind misinterprets it).

## Spacing & Layout

- **Hardcode pixel values from Figma per breakpoint.** This project uses fixed-breakpoint design (see `responsive-layout.mdc`). Use `gap-[6px] md:gap-2`, not `gap-[clamp(6px,…,8px)]`.
- **Only use `clamp()` / `vw` when Figma explicitly designs fluid behaviour** (e.g. an offer rail that bleeds to the right edge of the viewport). When you do, document the source Figma node and the reason in the section's `constants.ts`.
- Prefer `ring-1 ring-inset` over `border` when a CSS border would shrink the inner content box and break Figma widths (we discovered this on `OfferCard`).
- Asymmetric padding from Figma must be reproduced asymmetrically. If Figma shows `pt: 12, pb: 16, px: 16`, use `pt-3 pb-4 px-4` — not `p-4` "because it averages out".

## Typography sizes

Font sizes from Figma are constant across breakpoints in this project (Heading L = 36 px everywhere, Body L = 16 px everywhere, Body XL = 20 px, Body M = 14 px). Differences in rendered height between breakpoints come from text wrapping due to available width, not from a different font size. Do **not** introduce per-breakpoint font sizes unless the Figma design genuinely changes them.
