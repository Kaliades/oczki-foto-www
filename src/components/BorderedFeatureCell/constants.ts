/** Copy cell inside vertical bordered feature lists — title body/l + description body/m. */
export const BORDERED_FEATURE_CELL_LAYOUT = {
  copyGapPx: 6,
  paddingDesktopTabletPx: 20,
  paddingMobileY: 16,
} as const
