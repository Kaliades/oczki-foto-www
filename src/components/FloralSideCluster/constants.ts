/** Absolute layout box for a floral `OBJECTS` cluster — values from Figma MCP metadata. */
export type FloralSideClusterLayout = {
  bottom?: number
  figmaNode?: string
  flipY?: boolean
  /** Stretch to the parent’s full height (top/bottom edges of the section). */
  fullHeight?: boolean
  height?: number
  left?: number
  right?: number
  src: string
  top?: number
  width: number
}
